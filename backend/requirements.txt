fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.1
# 2.0.30 predates Python 3.13's `__firstlineno__` class attribute and raises
# "Can't replace canonical symbol for '__firstlineno__'" on import, which stops
# the panel dead on Ubuntu 26.04 (Python 3.14). Same 2.0 series, so no API
# change; verified on 3.14 and re-run against the suite on 3.12.
sqlalchemy==2.0.54
pydantic-settings==2.3.1
email-validator==2.1.1
python-jose[cryptography]==3.5.0
redis==7.4.0
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
# Python 3.13 removed the crypt module (PEP 594). This binds the system
# libcrypt so shadow hashes - including yescrypt, which passlib cannot do -
# still verify on Ubuntu 26.04 and later. Unused on 3.12 and earlier.
legacycrypt==0.3
python-multipart==0.0.32
jinja2==3.1.6
PyYAML==6.0.3
cryptography==50.0.0
alembic==1.13.2
pyotp==2.9.0
qrcode[pil]==7.4.2
paramiko==3.4.0
maxminddb==3.1.1
