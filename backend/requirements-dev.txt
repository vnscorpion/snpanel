# Test-only dependencies.
#
# Kept apart from requirements.txt because a panel installation must not carry
# a test client. Declared at all because the suite was relying on httpx being
# present for other reasons: starlette's TestClient needs it, nothing asked
# for it, and so `pytest` passed on a developer's machine and failed on a
# clean checkout with
#
#     RuntimeError: The starlette.testclient module requires the httpx2
#     package to be installed
#
# starlette 1.6 wants httpx2 specifically; plain httpx still works but warns
# that it is deprecated for this use.
pytest==8.4.2
httpx2==2.13.0
